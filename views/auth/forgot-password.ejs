<%- include('../partials/header') %>

<div class="auth-page">
  <div class="auth-card">
    <div style="text-align:center;margin-bottom:24px;font-size:40px">🔑</div>
    <h1 class="auth-title">نسيت كلمة المرور؟</h1>
    <p class="auth-sub">أدخل بريدك الإلكتروني وسنرسل لك رابط إعادة تعيين كلمة المرور</p>

    <% if (error) { %>
      <div class="alert alert-error">⚠️ <%= error %></div>
    <% } %>
    <% if (success) { %>
      <div class="alert alert-success">✅ <%= success %></div>
    <% } %>

    <% if (!success) { %>
    <form method="POST" action="/auth/forgot-password">
      <div class="form-group">
        <label class="form-label">البريد الإلكتروني</label>
        <input type="email" name="email" class="form-input" placeholder="example@email.com" required>
      </div>
      <button type="submit" class="btn-submit">إرسال رابط الاستعادة →</button>
    </form>
    <% } %>

    <p style="text-align:center;margin-top:20px;font-size:13px;color:var(--text-secondary);">
      <a href="/auth/login" style="color:var(--accent-gold);text-decoration:none;">← العودة لتسجيل الدخول</a>
    </p>
  </div>
</div>

<%- include('../partials/footer') %>
